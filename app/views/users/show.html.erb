<h1>Your profile details</h1>
<%= form_for(@user) do |f| %>

    <div class="field">
      Name :
      <%= @user.name %>
    </div>
    <div class="field">
      Email:
      <%= @user.email%>
    </div>

<% end %>

<%= link_to 'Back to Room Booking', rooms_url%>

<p>Login now</p>
<%= link_to 'Login', login_path %>